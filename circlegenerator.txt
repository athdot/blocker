<body>
	<p id="multiples"></p>
	<script>
		function goMultiple(num){
			var run = 1;
			while(run < num){
				var who = num/run;
				var numy = num;
				var firun = run;
				var who2 = Math.floor(who);
				if(who == who2){
					var divisi = (numy/firun)/firun;
					var divisi2 = Math.floor(divisi);
					if(divisi == divisi2){
						var numys = divisi;
						var firun = firun/(who/divisi);
					}else{
						var numys = numy/firun;
					}
					if(numys < firun){
						var newmys = numys;
						var numys = newmys/(firun/(firun/newmys));
						var firun = firun/newmys;
						var why = firun;
						var whoy = Math.floor(why);
						if(why != whoy){
							var dec = why-whoy;
							var mul = 1/dec;
							var numys = numys*mul;
							var firun = firun*mul;
						}
					}
					var pagewidth = window.innerWidth;
					var pageheight = window.innerHeight;
					if(pagewidth > pageheight){
						var lel = pageheight/pagewidth;
						var lele = Math.floor(lel);
						if(lel != lele){
							var dece = lel-lele;
							var mule = 1/dece;
							var ratx = lel*mul;
							var raty = 1*mul;
						}else{
							var ratx = lel;
							var raty = 1;
						}
					}else if(pageheight > pagewidth){
						var lel = pagewidth/pageheight;
						var lele = Math.floor(lel);
						if(lel != lele){
							var dece = lel-lele;
							var mule = 1/dece;
							var raty = lel*mul;
							var ratx = 1*mul;
						}else{
							var raty = lel;
							var ratx = 1;
						}
					}else{
						var returnme = "1:1";
					}
					return(numys + ":" + firun);
				}
				run++;
			}
		}
var lol = goMultiple(100);
document.write(lol);
	</script>
</body>
